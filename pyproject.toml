[project]
name = "email-declutter"
version = "0.1.0"
description = "OpenEnv-compatible email triage environment"
requires-python = ">=3.10"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openenv>=0.2.0"
]

[project.scripts]
server = "main:app"
