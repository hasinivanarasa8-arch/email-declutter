[project]
name = "email-declutter"
version = "0.1.0"
description = "OpenEnv-compatible email triage environment"
requires-python = ">=3.10"

dependencies = [
    "fastapi>=0.104.0",
    "uvicorn>=0.24.0",
    "pydantic>=2.0.0",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "main:server"